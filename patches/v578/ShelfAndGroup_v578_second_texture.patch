--- a/app/src/main/java/com/simplereader/app/ui/MainActivity.kt
+++ b/app/src/main/java/com/simplereader/app/ui/MainActivity.kt
@@ -73,6 +73,11 @@ class MainActivity : AppCompatActivity() {
     private val coverBitmapCache = object : LruCache<Long, Bitmap>(12 * 1024) {
         override fun sizeOf(key: Long, value: Bitmap): Int = (value.byteCount / 1024).coerceAtLeast(1)
     }
+    private val paperTextureBitmap: Bitmap by lazy(LazyThreadSafetyMode.NONE) {
+        requireNotNull(BitmapFactory.decodeResource(resources, R.drawable.paper_texture_v577)) {
+            "纸质封面纹理资源缺失"
+        }
+    }
 
     private val openFolderLauncher = registerForActivityResult(
         ActivityResultContracts.OpenDocumentTree()
@@ -404,12 +409,13 @@ class MainActivity : AppCompatActivity() {
             gravity = Gravity.CENTER
             maxLines = if (compact) 4 else 5
             setPadding(dp(6), dp(8), dp(6), dp(8))
-            background = GradientDrawable(
-                GradientDrawable.Orientation.TOP_BOTTOM,
-                intArrayOf(Color.rgb(74, 126, 172), Color.rgb(47, 94, 136))
-            ).apply {
-                cornerRadius = dp(if (compact) 3 else 5).toFloat()
-            }
+            background = PaperCoverDrawable(
+                textureBitmap = paperTextureBitmap,
+                topColor = Color.rgb(74, 126, 172),
+                bottomColor = Color.rgb(47, 94, 136),
+                radiusPx = dp(if (compact) 3 else 5).toFloat(),
+                seed = (book.id xor (book.id ushr 32)).toInt()
+            )
         }
         val image = ImageView(this).apply {
             visibility = View.GONE
--- a/app/src/main/java/com/simplereader/app/ui/GroupBooksActivity.kt
+++ b/app/src/main/java/com/simplereader/app/ui/GroupBooksActivity.kt
@@ -3,6 +3,7 @@ package com.simplereader.app.ui
 import android.content.Intent
+import android.graphics.Bitmap
 import android.graphics.Color
 import android.graphics.BitmapFactory
 import android.graphics.drawable.GradientDrawable
@@ -21,6 +22,7 @@ import androidx.appcompat.app.AppCompatActivity
 import androidx.lifecycle.lifecycleScope
 import androidx.recyclerview.widget.GridLayoutManager
 import androidx.recyclerview.widget.RecyclerView
+import com.simplereader.app.R
 import com.simplereader.app.data.cache.StructuredBookCache
 import com.simplereader.app.data.db.SimpleReaderDatabase
 import com.simplereader.app.data.model.ShelfBookItem
@@ -37,6 +39,11 @@ class GroupBooksActivity : AppCompatActivity() {
     private lateinit var rootView: LinearLayout
     private var groupId: Long = 0L
     private var groupName: String = ""
+    private val paperTextureBitmap: Bitmap by lazy(LazyThreadSafetyMode.NONE) {
+        requireNotNull(BitmapFactory.decodeResource(resources, R.drawable.paper_texture_v577)) {
+            "纸质封面纹理资源缺失"
+        }
+    }
 
     override fun onCreate(savedInstanceState: Bundle?) {
         AppTheme.apply(this)
@@ -53,6 +60,7 @@ class GroupBooksActivity : AppCompatActivity() {
 
         bookRepository = BookRepository(SimpleReaderDatabase.getDatabase(this).bookDao())
         adapter = GroupBookAdapter(
+            textureBitmap = paperTextureBitmap,
             onOpen = { openBook(it.id) },
             onLongPress = { showBookActions(it) }
         )
@@ -186,6 +194,7 @@ class GroupBooksActivity : AppCompatActivity() {
     }
 
     private class GroupBookAdapter(
+        private val textureBitmap: Bitmap,
         private val onOpen: (ShelfBookItem) -> Unit,
         private val onLongPress: (ShelfBookItem) -> Unit
     ) : RecyclerView.Adapter<GroupBookAdapter.BookViewHolder>() {
@@ -198,7 +207,7 @@ class GroupBooksActivity : AppCompatActivity() {
         }
 
         override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): BookViewHolder {
-            return BookViewHolder(BookCardView(parent))
+            return BookViewHolder(BookCardView(parent, textureBitmap))
         }
 
         override fun onBindViewHolder(holder: BookViewHolder, position: Int) {
@@ -224,7 +233,10 @@ class GroupBooksActivity : AppCompatActivity() {
         }
     }
 
-    private class BookCardView(parent: ViewGroup) : LinearLayout(parent.context) {
+    private class BookCardView(
+        parent: ViewGroup,
+        textureBitmap: Bitmap
+    ) : LinearLayout(parent.context) {
         private val coverFallback = TextView(context)
         private val coverImage = ImageView(context)
         private val title = TextView(context)
@@ -247,12 +259,13 @@ class GroupBooksActivity : AppCompatActivity() {
                 textSize = 11f
                 setTextColor(Color.rgb(232, 238, 244))
                 setPadding(dp(6), dp(8), dp(6), dp(8))
-                background = GradientDrawable(
-                    GradientDrawable.Orientation.TOP_BOTTOM,
-                    intArrayOf(Color.rgb(74, 126, 172), Color.rgb(47, 94, 136))
-                ).apply {
-                    cornerRadius = dp(5).toFloat()
-                }
+                background = PaperCoverDrawable(
+                    textureBitmap = textureBitmap,
+                    topColor = Color.rgb(74, 126, 172),
+                    bottomColor = Color.rgb(47, 94, 136),
+                    radiusPx = dp(5).toFloat(),
+                    seed = 0
+                )
             }
             coverImage.apply {
                 visibility = View.GONE
