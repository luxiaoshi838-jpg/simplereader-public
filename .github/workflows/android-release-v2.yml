name: Build 简阅 v775

on:
  push:
    branches:
      - source-v775
  workflow_dispatch:

permissions:
  contents: write

env:
  SIMPLE_READER_VERSION_CODE: "2098000775"
  SIMPLE_READER_VERSION_NAME: "775"
  APK_PATH: "app/build/outputs/apk/release/SimpleReader_v775_github_unsigned.apk"
  MAX_APK_BYTES: "52428800"

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: source-v775
          fetch-depth: 0
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4

      - name: Preserve v773 startup-safe shelf fast-scroll fix
        shell: bash
        run: python3 tools/apply-v773-startup-safe-fastscroll.py

      - name: Preserve v774 A-style icon resources and state binding
        shell: bash
        run: python3 tools/apply-v774-day-night-icons.py

      - name: Apply v775 single-image-slot day/night fix
        shell: bash
        run: python3 tools/apply-v775-single-mode-icon.py

      - name: Persist v775 runtime source before validation
        shell: bash
        run: |
          if git diff --quiet -- \
            app/build.gradle.kts \
            app/src/main/java/com/simplereader/app/ui/DayNightModeIcon.kt \
            app/src/main/java/com/simplereader/app/ui/MainActivity.kt \
            app/src/main/java/com/simplereader/app/ui/ReaderActivity.kt \
            app/src/main/java/com/simplereader/app/ui/GroupBooksActivity.kt \
            app/src/main/res/layout/activity_main.xml \
            app/src/main/res/layout/activity_reader.xml; then
            echo 'v775 runtime source already persisted.'
          else
            git config user.name github-actions[bot]
            git config user.email 41898282+github-actions[bot]@users.noreply.github.com
            git add \
              app/build.gradle.kts \
              app/src/main/java/com/simplereader/app/ui/DayNightModeIcon.kt \
              app/src/main/java/com/simplereader/app/ui/MainActivity.kt \
              app/src/main/java/com/simplereader/app/ui/ReaderActivity.kt \
              app/src/main/java/com/simplereader/app/ui/GroupBooksActivity.kt \
              app/src/main/res/layout/activity_main.xml \
              app/src/main/res/layout/activity_reader.xml
            git commit -m 'v775：持久化单一日太阳夜月亮图标 [skip ci]'
            git push origin HEAD:source-v775
          fi

      - run: chmod +x ./gradlew tools/v770-52-gates.sh tools/v770-selection-touch-gates.sh tools/v770-instant-selection-gates.sh tools/v767-settings-layout-gates.sh tools/v770-full-unit-baseline-gate.sh tools/v773-startup-fastscroll-gates.sh tools/v775-single-mode-icon-gates.sh tools/v775-emulator-launch-test.sh

      - name: Run inherited 52 stability gates
        run: bash tools/v770-52-gates.sh
      - name: Run inherited selection/touch coexistence gates
        run: bash tools/v770-selection-touch-gates.sh
      - name: Run inherited instant selection activation gates
        run: bash tools/v770-instant-selection-gates.sh
      - name: Run compact settings-row layout gate
        run: bash tools/v767-settings-layout-gates.sh
      - name: Run v775 single-mode icon semantics and startup gates
        run: bash tools/v775-single-mode-icon-gates.sh

      - name: Verify source diff formatting
        run: git diff --check

      - name: Run full unit suite against unchanged V758 failure baseline
        shell: bash
        run: bash tools/v770-full-unit-baseline-gate.sh v775-full-unit-tests.log

      - name: Build v775 release
        shell: bash
        run: |
          set -o pipefail
          ./gradlew assembleRelease --stacktrace 2>&1 | tee gradle-build.log

      - name: Enable KVM group access
        shell: bash
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm || true

      - name: Android 35 real Release cold-launch gate
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          arch: x86_64
          profile: pixel_6
          disable-animations: true
          emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          script: bash tools/v775-emulator-launch-test.sh

      - name: Verify v775 APK and export official signer
        shell: bash
        run: |
          set -euo pipefail
          src="app/build/outputs/apk/release/app-release-unsigned.apk"
          [ -f "$src" ] || src="app/build/outputs/apk/release/app-release.apk"
          cp "$src" "$APK_PATH"
          build_tools="$(find "$ANDROID_HOME/build-tools" -mindepth 1 -maxdepth 1 -type d | sort -V | tail -n 1)"
          "${build_tools}/aapt2" dump badging "$APK_PATH" | tee apk-package-info.txt
          grep -Fq "package: name='com.simplereader.app'" apk-package-info.txt
          grep -Fq "versionCode='2098000775'" apk-package-info.txt
          grep -Fq "versionName='775'" apk-package-info.txt
          grep -Fq "minSdkVersion:'26'" apk-package-info.txt
          test "$(stat -c %s "$APK_PATH")" -le "$MAX_APK_BYTES"
          sha256sum "$APK_PATH" | tee apk-sha256.txt
          mkdir -p signing-tools/lib
          cp "${build_tools}/apksigner" signing-tools/apksigner
          cp "${build_tools}/lib/apksigner.jar" signing-tools/lib/apksigner.jar
          chmod +x signing-tools/apksigner

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: SimpleReader-v775-${{ github.run_number }}
          path: |
            ${{ env.APK_PATH }}
            apk-sha256.txt
            apk-package-info.txt
            v775-full-unit-tests.log
            gradle-build.log
            v775-emulator-signature.txt
            v775-emulator-install.txt
            v775-emulator-start.txt
            v775-emulator-logcat.txt
            v775-emulator-activity.txt
            v775-emulator-pid.txt
            v775-emulator-result.txt
            signing-tools/apksigner
            signing-tools/lib/apksigner.jar
          if-no-files-found: warn
          retention-days: 7
