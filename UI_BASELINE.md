# 简阅阅读 UI 硬性基线

只允许使用已经确认的实现。任何旧版本、历史分支、PR、构建产物或文档都不能因为“以前存在过”而被当作恢复依据。

## 阅读页几何与上下栏

- 阅读正文顶部和底部始终保留固定安全边界：状态栏/通知栏下方一个完整字符高度，导航栏/屏幕底部上方一个完整字符高度。
- 顶部 ActionBar 必须使用 overlay；底部 `readerControls` 必须作为覆盖层存在。
- 上下栏显示/隐藏不得改变正文高度、宽度、margin、padding、分页参数或 scrollY，不得触发 `requestLayout()`、重新分页或位置补偿。
- `readerControls` 隐藏使用 `INVISIBLE`，禁止 `GONE ↔ VISIBLE` 造成阅读区尺寸变化。
- 禁止为了实现覆盖栏而删除正文原有上下安全边界；正文不得延伸到状态栏、导航栏或屏幕物理边缘。

## 封面

- EPUB 优先显示真实 EPUB 封面；没有真实封面时使用确认的 EPUB 默认封面。
- TXT 及其他非 EPUB 书籍使用真实 v625 中确认的 `book_cover_default_txt.webp` 默认封面。
- 禁止无封面、空白卡片、纯文字卡片、临时占位封面或 `book_cover_default_generic.webp` 一类替代实现。
- 禁止程序绘制“TXT”字样或交叉线来伪造封面。

## 阅读背景

- 背景严格使用真实 v625 的**单类别、单背景**结构：`ReaderBackgrounds.Selection(category, optionId)`。
- “纯色 / 纹理 / 质感”是三个互斥类别；用户每次只选择一个完整背景方案，禁止三层自由组合或叠加。
- 背景使用对应的完整 v625 背景位图按阅读区域等比中心裁切。
- 禁止 `LayeredReaderBackgroundDrawable`、`BitmapShader`、`TILED_BITMAP`、REPEAT 平铺、缩略图放大、程序生成圆点/纸纤维、把一种纹理冒充多种纹理、纯色替代缺失素材。
- 唯一允许的二进制背景/封面素材及 SHA-256 记录在 `REAL_V625_ASSET_ALLOWLIST.md`；任何素材缺失或哈希不一致时必须阻止构建/发布，不能回退到旧背景实现。

## 阅读交互与分页

- 目录/书签使用已确认的左侧全高面板，顶部只有“目录 / 书签”；添加书签仅在阅读页上栏右上角圆形“添”。
- 横向翻页使用确认版 `PagedReaderView`；覆盖、仿真、平移必须保持不同渲染行为。
- 上下阅读保持连续阅读体验；禁止 RecyclerView/PagerSnapHelper 式整页吸附和页间分割。
- 大文件上下阅读允许使用有限文本窗口保障性能，但换窗必须保持当前文本锚点和屏幕像素位置，不能跳回页首或章节首。
- 横向分页按真实章节和固定排版参数生成；章节最后一页只允许因正文不足自然留白，禁止额外人为大底部空白。
- 章节标题只根据真实章节起点处理，比正文固定大 2sp 并加粗；禁止对普通正文猜测标题后再次套用章节样式。
- 章节跳转必须进入目标章节真实起点并立即绑定前页、当前页、后页。
- 右下角页码只显示纯文字，禁止半透明白色方块、底板或卡片背景。
- 阅读上栏显示当前章节名，不显示书籍名；目录面板标题为“目录　书籍名”。

## 目录与缓存

- TXT 目录识别先使用标准、带前缀和结构词规则；整本未识别出结构标题时才启用无标点独立短行兜底。兜底不得把带标点句子或明显普通正文句误识别为目录。
- 书架目录缓存必须提供“全书架目录缓存”和“全书架无目录书籍缓存”两个选项，缓存范围由用户明确决定。
- 目录规则版本只负责判定旧识别结果是否有效，不得在打开书籍或目录时自动替用户启动全书架重建。
- 分页缓存按书籍和版式分别保存，任何版式不得覆盖或删除其他版式的有效缓存。

## 永久禁止恢复

- `ReaderCatalogSheet`、`ReaderSurfaceDrawable`、`ReaderPageAdapter`、`VerticalPageFlowView`。
- 阅读页 `RecyclerView/PagerSnapHelper` 整页吸附路线。
- 上下栏压缩阅读区实现。
- 无封面/空白封面实现。
- 正文侵入系统栏的无上下边界全屏实现。
- 颜色+纹理+质感三层叠加、平铺、程序生成或素材替代背景实现。

任何后续修改先通过 `tools/verify-rejected-reader-implementations.sh`。发现上述实现时直接失败，不允许以兼容、回退、临时方案或旧版本恢复为理由绕过。