# 简阅阅读 UI 硬性基线

仅允许使用已经确认的实现，不得自行重写或以新类替代。

- 目录/书签：使用 v600 左侧全高面板，顶部只有“目录 / 书签”；添加书签仅在阅读页上栏右上角圆形“添”。
- 阅读背景：使用确认版 `ReaderBackgrounds` 与 `ReaderBackgroundPicker`，颜色、纹理、质感三层分别选择并叠加。
- 横向翻页：使用确认版 `PagedReaderView`；覆盖、仿真、平移必须是不同渲染行为。
- 上下阅读：整本正文是一个连续整体，只使用一个连续 TextView；禁止页面容器、分页吸附和页间分割。
- 章节间距：只允许真实章节之间增加少量垂直间距；禁止在每页之间增加空白。
- 横向分页：章节独立分页，只有章节最后一页因正文不足自然留白；每页额外底部预留必须为 0。
- 章节标题：只根据真实章节起点处理，禁止对正文中的疑似标题再次套用章节标题样式。
- 章节跳转：跳转后必须立即重新绑定前页、当前页和后页，不得卡住或让标题漂移到其他页。
- 禁止恢复：`ReaderCatalogSheet`、`ReaderSurfaceDrawable`、`ReaderPageAdapter`、`VerticalPageFlowView`、阅读页 `RecyclerView/PagerSnapHelper`。

- 右下角页码只能显示纯文字，禁止半透明白色方块、底板或卡片背景。
- 阅读区顶部：以 v612 阅读页整体边界为基准，不得移动整个根布局；正文顶部和底部均保留一个字符高度（24dp），不得贴住通知栏或屏幕底边。
- 章节标题：仅真实章节首行，比正文固定大 2sp，并保持加粗。
- 阅读上栏：显示当前章节名，不显示书籍名；目录面板标题为“目录　书籍名”。
- TXT 目录识别：先使用标准、带前缀和结构词规则；整本未识别出结构标题时才启用无标点独立短行兜底。兜底不得把带标点句子或明显普通正文句误识别为目录。
- 书架目录缓存：点击后必须提供“全书架目录缓存”和“全书架无目录书籍缓存”两个选项。前者处理全部书籍；后者仅跳过当前文件名、文件大小未变化且已经识别出目录的书籍。两种模式都必须使用 WorkManager 前台后台任务缓存目录与分页，阅读或切换到其他应用不影响任务。
- 缓存范围必须由用户点击上述两个选项明确决定；目录规则版本变化只负责判定旧识别结果是否有效，不得自动替用户改成全书架缓存。

- 默认封面：普通文本只使用用户提供的通用无格式标识封面，公开库禁止存在带 TXT 标识的封面资源或绘制逻辑；EPUB 使用用户提供的橙色默认封面；EPUB 检测到真实封面时必须显示真实封面，不得叠加或启用默认 EPUB 封面。
- 阅读背景素材：基础纹理必须直接使用上传包中的 1080×2340 完整纹理图并按页面等比裁切；禁止使用 97×97 选择器缩略图放大平铺，禁止程序生成圆圈、纸纤维或以纯色替代纹理。
- 目录标题格式：支持“连续汉字 + 紧接括号数字”，例如“大道之上（1）”“大道之上（一）”；括号前不得含空格，前缀不得是英文或普通混合句。
- 目录缓存有效性：目录规则版本只记录来源，不得在打开书籍或目录时自动触发重新识别。重新识别范围只能由“全书架目录缓存”或“全书架无目录书籍缓存”明确决定。
- 闪退日志：未捕获异常必须保存到应用私有目录；下一次进入书架弹出“闪退/崩溃日志”，只有复制成功后才清除，暂不复制时下次仍需出现。

- 大文件上下阅读：禁止把整本正文一次性放入单个 TextView/StaticLayout；必须使用有限页窗口并在接近上下边界时换窗。
- 分页缓存：同一本书不同版式分别保存，任何版式不允许覆盖或删除其他版式的有效缓存。

- 阅读上下栏覆盖规则（永久）：上栏、下栏都必须是 `readerRoot`/`FrameLayout` 内的覆盖层，显示或隐藏时禁止改变 `readerRoot`、`readerScrollView`、`pagedReaderView` 的高度、padding、margin 或布局测量结果。
- 阅读上下栏禁止恢复（永久）：`ReaderActivity` 禁止使用 `supportActionBar.show()/hide()` 作为阅读上栏；禁止因阅读上下栏显示/隐藏或其 inset 变化调用 `paginateAndDisplay()`、`renderContinuousWindow()`、`scrollTo()`、`jumpToPage()` 做位置补偿。
- 系统栏 inset 只允许用于正文固定安全边距，以及覆盖式上栏/下栏自身的 topMargin/bottomMargin；不得通过 inset 改变阅读正文容器尺寸。
