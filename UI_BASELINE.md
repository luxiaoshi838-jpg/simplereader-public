# 简阅 UI 硬性基线

本文件是公开库唯一有效的 UI 约束。分页、缓存、目录映射、搜索和书签定位修改不得绕过本基线。

## 永久禁止恢复的实现

- 十字纹、规则横线或规则竖线组成的书籍封面。
- 封面上的 `TXT`、`EPUB`、`CHM` 等格式文字。
- 旧 `ReaderPanels.kt` 目录/书签弹窗。
- 在目录/书签面板内放置“添加书签”。添加书签只能位于阅读页上栏右上角。
- 只有纯色、没有纹理和质感层的阅读背景。
- 将覆盖、仿真、平移三个模式接到同一种滚动或动画实现。
- 把阅读页旧的 118dp 底部工具栏预留用于每一个分页页面。
- 任何 v14 专用工作流、补丁或构建状态文件。

## 当前有效实现

- 封面：颜色层、柔和材质光影、非规则纸张纤维/颗粒；没有格式文字和规则网格。
- 阅读背景：颜色、纹理、质感三层叠加；更换背景颜色不重新分页。
- 目录与书签：独立底部面板，仅有“目录/书签”两个标签；书签创建仍在阅读页上栏右上角。
- 分页：普通页面使用完整阅读区域；只有章节末页内容不足时自然留白。
- 翻页：覆盖、仿真、平移、上下和淡入使用不同实现。

## 合并规则

`tools/verify-ui-policy.sh` 与 `ui-lock.sha256` 必须通过。除非项目所有者明确要求修改 UI，否则 UI 锁定文件不得更新哈希。
