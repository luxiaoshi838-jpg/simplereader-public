# TXT 目录识别维护日志

## 2026-08-19 — V682 之后固定维护方式

### 当前真实目录识别入口

TXT 目录规则后续不要再分散修改旧的 `priority / secondFamily / ReaderChapterPrioritySelector` 多层选择链。
当前稳定方案以 **V654 为应用基线**，使用独立的最终 TXT 目录生成器 `DirectTxtCatalogV100`，TXT 实际加载入口直接调用该生成器，将识别结果作为最终章节列表。

### 后续要修改目录识别时，只改这几个位置

1. **最终目录规则本体**
   - 修改 `DirectTxtCatalogV100` 中的逐行判断逻辑。
   - 不再去同时修改旧的 `ChapterPriorityClassifier`、`ReaderChapterPrioritySelector`、旧 priority 家族选择逻辑，避免规则在多层之间不一致。

2. **目录显示名规范化**
   - V681 起，识别成功后的标题先经过 `CatalogTitleNormalizerV103`，再写入 `BookChapter.title`。
   - 该层只清洗目录显示名称，不修改正文内容，也不修改章节 `startOffset/endOffset`。
   - 首尾空白删除；Tab、全角空格、NBSP/Unicode 空白统一处理；中间连续空白压缩为一个普通空格。
   - 被空白拆开的章节编号内部要合并，例如 `第五十          三章激情飙车` 显示为 `第五十三章激情飙车`；`第 0 1 章` 显示为 `第01章`。
   - 括号编号内部多余空白同样清理，例如 `（   一   ） 标题` 显示为 `（一） 标题`。

3. **正文章节标题渲染规范化（V682 起）**
   - 新增 `ReaderBodyTitleNormalizerV104`，只在阅读页把 `ReaderPageSnapshot` 内容交给 `TextView` 之前规范化章节标题显示。
   - 当前页、前一页、后一页均通过同一渲染规范化入口，避免翻页后旧空白重新出现。
   - 优先按当前目录识别规则判断章节标题；如果页面内容已带章节标题的 `RelativeSizeSpan`，即使原文中的空白把编号拆开，也仍按章节标题处理。
   - 例如正文原文 `第五十          三章激情飙车`，页面显示为 `第五十三章激情飙车`；`第 0 1 章` 显示为 `第01章`。
   - **只改渲染字符串，不修改原 TXT、ReaderDocument.text、章节 startOffset/endOffset、分页锚点、搜索源文本和阅读进度。**
   - 如果原页面内容带 Spannable 样式，规范化后必须同步迁移 span，保留章节标题放大等样式。

4. **目录规则版本**
   - 每次目录识别规则或目录显示规范化规则变化必须递增 `TxtParser.CATALOG_RULE_VERSION`。
   - V681 当前为 `103`。V682 仅增加正文渲染层，不改变目录识别结果，因此目录规则版本仍保持 `103`。
   - 规则版本仅用于判断旧目录是否需要重新识别；版本一致时不得每次打开都重复扫描。

5. **缓存失效必须和规则版本绑定**
   - normalized TXT 缓存（包括内存与 `txt-source.json`）读取时必须检查 `catalogRuleVersion`。
   - `recognition.json` 同样必须检查当前目录规则版本。
   - 分页缓存中保存的 `ReaderBook.chapters` 不能覆盖新版目录；目录规则升级时必须让使用旧目录生成的分页章节缓存失效一次。
   - 不要修改分页缓存数据格式本身来达到失效目的，避免再次出现“可读模式 / 无页数 / 目录打不开”。
   - V682 的正文渲染规范化发生在 `ReaderPageSnapshot -> TextView` 之间，不要求清理或重建分页缓存。

6. **规则升级后的打开逻辑**
   - 若该书缓存目录版本 `< 当前 CATALOG_RULE_VERSION`：重新识别该书 TXT 目录并覆盖写入新版本。
   - 若版本 `== 当前 CATALOG_RULE_VERSION`：直接使用缓存，不重复识别。
   - 不需要“全书架生成目录”功能。

### 当前章节规则要求

所有允许的结构化格式必须可以在同一本书中同时存在，包括但不限于：

- `第一章 / 第1章 / 第10章 / 第01章 / 第001章`
- `第一话 / 第1话 / 第10话 / 第01话`
- `第一节 / 第1节`
- `第一回 / 第1回`
- `一xxxx / 1xxxx / 01xxxx / 10xxxx`
- `一.xxxx / 1.xxxx`
- `（一）xxx / （1）xxx / （01）xxx`
- `（一）.xxx / （1）.xxx`
- `xxx（一） / xxx（1）`
- `xxx一 / xxx十 / xxx十一 / xxx一百零一`
- `xxx1 / xxx01 / xxx10 / xxx100`
- 例如 `xxx，xxx01`、`xxx xxx10` 也允许。

### 位置规则

允许出现在标题中间并仍作为章节标记的包括：

- `第N章`
- `第N话`
- `第N回`
- `（N）`

其中 `第N章 / 第N话` 明确允许出现在任意位置，例如：

- `第一部 xxx 第01章 xxxx xxxx`
- `xxx 第001章 xxxx`
- `xxx 第10话 xxxx`

只要整行满足长度和终止符规则即可识别。

`第N节` 不允许位于标题中间。
裸数字或裸中文数字只能位于标题开头或结尾；如 `abc10def`、`abc十一def` 不得识别。

### 标点规则

以下六个符号作为句子终止符处理：

- `。`
- `.`
- `？`
- `?`
- `！`
- `!`

位于标题中间的 `第N章 / 第N话 / 第N回 / （N）` 若整行包含这些终止符，应拒绝整行。
其他结构化格式继续按既定终止符规则处理。

### 标题长度规则（V680 起）

- 章节名最多 **25 个非空白字符**。
- 空格、Tab、换行等空白字符 **不计入 25 字限制**。
- 25 个非空白字符允许；26 个及以上直接拒绝。
- 该限制用于拦截没有句号、问号、感叹号但实际上是正文长句的误识别。
- 示例 `第一部 xxx 第01章 xxxx xxxx` 按非空白字符计算为 18，允许识别。

### V681/V682 标题空白修正规则

- 识别阶段仍按 V680 的 25 个非空白字符规则执行，不因为清洗显示名而改变识别范围。
- V681：命中后规范化目录显示名。
- V682：阅读页渲染章节标题行时做同样的空白规范化。
- 目录和正文显示中不得保留章节标题原 TXT 行首的大段缩进、Tab、全角空格或连续空格。
- 所有规范化都只作用于显示层，章节跳转位置继续使用原始行的真实 `startOffset`。

### 真实样本回归要求

发布下一版前必须用《门房秦大爷的故事》真实 TXT 做回归，至少确认最终章节列表中存在：

- `秦大爷的故事1`
- `秦大爷的故事9`
- `秦大爷的故事10`
- `秦大爷的故事11`
- `秦大爷的故事20`
- `秦大爷的故事21`
- `外篇 刘小静篇 01`
- `外篇 刘小静篇 02`
- `外篇 刘小静篇 03`

并确认这些章节不是只在正则测试中命中，而是进入**最终章节列表**。

### 发布验收禁止项

以后不得再用下面这些单独作为“目录已经修好”的依据：

- 单条正则 PASS
- `priority()` 返回正确
- `dexdump` / D8 通过
- 目录规则版本已递增

这些只能作为技术检查，不能代替最终目录列表验收。

最终必须同时验证：

1. 真实 TXT 最终章节列表正确；
2. 目录面板可打开；
3. 右下角页数正常；
4. 阅读进度恢复正常；
5. 不出现“可读模式”降级；
6. 新增类及其内部类全部真实存在于最终 DEX，避免再次出现 `NoClassDefFoundError`；
7. 目录标题空白规范化正确，且不改变正文和章节 offset；
8. 正文章节标题渲染空白规范化正确，且原始正文、分页锚点、搜索和进度数据不被改写。

### 基线要求

如果后续再次大改目录逻辑，优先从稳定 APK 基线重新制作，不要继续在大量历史补丁版上叠补丁。当前这轮目录重构的应用基线是 **V654**。
